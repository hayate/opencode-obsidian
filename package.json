{
  "name": "opencode-superpower-obsidian",
  "version": "0.1.0",
  "description": "Vault-backed memory, handoffs, specs and plans for OpenCode, with the superpowers process skills",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=22.23" },
  "scripts": {
    "test": "node --test \"tests/core/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "yaml": "^2.9.1"
  },
  "devDependencies": {
    "@types/node": "^24.13.6",
    "typescript": "^7.0.2"
  }
}
